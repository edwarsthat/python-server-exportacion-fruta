# Clave AES para encriptar/desencriptar archivos
# Debe ser hexadecimal de 64 caracteres (= 32 bytes = AES-256)
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_KEY=pon_aqui_tu_clave_hex_de_64_caracteres

# Configuración del servidor TCP
HOST=0.0.0.0
PORT=65432

# Otras
DEBUG=False
UPLOAD_PATH=./uploads
