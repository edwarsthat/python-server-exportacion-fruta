services:
  python-server:
    build: .
    ports:
      - "65432:65432"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ../storage:/storage
    restart: unless-stopped
